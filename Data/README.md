### data/
Contains the satellite imagery used in the project. The `raw/` folder stores the original GeoTIFF file exported from Google Earth Engine, while `tiles/` contains the processed image tiles generated from the preprocessing pipeline.
